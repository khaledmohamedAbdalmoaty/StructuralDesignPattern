package com.basePackage_khaled.Adapter;

public class Main {
    public static void main(String[]args){
        ImageView imageView=new ImageView();
        imageView.getImage(new Image());
       // imageView.apply(new VivdeFilter());
        imageView.apply(new avaFilter(new avaLibarary()));

    }
}
